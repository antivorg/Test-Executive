
window:
  layout:

thread_library:

io_objects:
  gui_in:
    # All GUI windget inputs will be place onto this queue
    type: Queue
  test_mutex:
    # Can be used by the user how they wish
    type: Lock
  stdin_queue:
    type: Queue
  stdout_queue:
    type: Queue
  stderr_queue:
    type: Queue

threads:
  0:
    name: Handle GUI inputs
    thread_definition: gui_input_handling
    io_binds: [gui_in, test_mutex]

file_descriptors: 
  0:
    name: Standard input
    label: stdin
    io_binds: [stdin_queue]
  1:
    name: Standard output
    label: stdout
    io_binds: [stdout_queue]
  2:
    name: Standard error
    label: stderr
    io_binds: [stderr_queue]
